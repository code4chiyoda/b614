---
slug: "5da7"
title: "千代田区市民機関データ仕様"
tracking_issue:
authors:
  - Nawashiro <rsm4905dc[a]mozmail.com>
status: draft
created: 2026-06-07
updated: 2026-06-07
---
# 千代田区市民機関データ仕様

> [!NOTE]
> 
> 本文書は Nawashiro が書いた草案です。改善できる箇所、より良い代替案を募集しています。

## 目的

本仕様は、千代田区内の市民機関・公共機関等の位置情報を含むデータセットを [Resource Description Framework](https://en.wikipedia.org/wiki/Resource_Description_Framework) として構築・維持するための識別子管理およびリンク修復手順を定義します。

本データセットは OpenStreetMap（以下 OSM）ID と概念の対応関係の維持を目的とします。OSM ID は永続識別子ではないため、独自の概念識別子（Concept ID）を導入します。

> [!TODO]
> 
> 現在の内容は OSM 対応に寄りすぎています。実際のところ、これは外部ソースとのリンク付けのためのものです。例えば公共図書館の場合、区のオープンデータと連携したほうが有益でしょう。

## 基本原則

### 識別子の階層

各機関は以下の識別子を持ちます。

| 種別          | 役割                     |
| ----------- | ---------------------- |
| Concept ID  | 施設概念の永続識別子             |
| External ID | 外部識別子（OSM 属性・GeoHash）  |
| OSM ID      | OSM のノード・ウェイ・リレーション ID |

Concept ID を主識別子とし、属性として外部識別子があります。OSM ID は外部識別子によって維持されます。

## データ構造

### Concept データ

施設概念を表現します。

保持項目:

* Concept ID
* 外部識別子
* OSM リンク情報

### OSM リンク情報

Concept と OSM オブジェクトの対応履歴を管理します。

保持項目:

* OSM ID
* リンク確立時刻

複数履歴を保持することで過去の OSM ID を参照可能にします。

> [!WARNING]
> 
> キャッシュのために [Overpass API](https://wiki.openstreetmap.org/wiki/JA:Overpass_API/Overpass_QL) をクエリするときは、先方や他者の迷惑にならないように気をつけてください。クエリをループすることは避けてください。
> 
> また、[newerフィルター](https://wiki.openstreetmap.org/wiki/JA:Overpass_API/Overpass_QL#%E5%A4%89%E6%9B%B4%E6%97%A5%E6%99%82%E3%81%AB%E3%82%88%E3%82%8B%E3%83%95%E3%82%A3%E3%83%AB%E3%82%BF%E3%83%BC_(changed)) など、差分更新を検討してください。

## Concept ID

### 要件

Concept ID は以下を満たさねばなりません。

* 永続的であること
* OSM ID から独立していること
* 人間による運用が容易であること

### 形式

> [!TODO]
> 
> 通し番号、ASCII 文字列、UUID などを検討すべきです。
> 
> 独自 ID にこだわりすぎるのは考えものです。Wikidata や公共施設 ID など、有益な一意の ID があればそちらも検討すべきです。
> 
> ID は必ずしもヒト可読を期待しませんが、管理する上でなにがなんだかよくわからないのは避けるべきです。
> 
> 一般的に UUID はやりすぎです。RDF として作成する都合上、ドメイン名前空間により、すでにグローバルに一意だからです。ただし、RSS / Atom フィード論争のようなこともあります。もしかしたら必要になるかもしれません。

## 外部識別子

### 目的

リンク修復に利用します。

### GeoHash

OSM オブジェクトの代表座標から算出します。

リンク修復の際、近傍の OSM データ抽出に使います。

### OSM ユニーク属性（任意）

Wikidata ID 等、その OSM データを一意に識別できる属性がある場合に使います。GeoHash よりも確実にリンクを修復できますが、一部の OSM オブジェクトしか対応していません。

運営者にも Wikidata ID が使われることがあり、それはユニークとして扱えないことに留意してください。

## 通常運用

利用者は Concept ID を参照します。

最新の有効な OSM リンクを取得し、外部サービスと連携を行います。

過去の OSM ID は削除せず履歴として保持します。これは Wheelmap 等の外部システムとの同期遅延に対応するためです。

## リンク切れ検出

OSM オブジェクトが削除されたなどの理由で Overpass クエリ応答がない場合、リンク切れとみなします。リンク修復を行います。

## 修復スキーム

### GeoHash スキーム

1. GeoHash 近傍を抽出
2. 以前の OSM オブジェクト属性と、近傍の OSM オブジェクト属性の編集距離を算出
3. 最適候補を選択

> [!TODO]
> 
> 現在は GeoHash を検討しています。ただし、必要なのは近傍を検索できることのみです。
> 
> GeoHash は高速な計算に向きますが、このシステムではむしろ通信による遅延を考慮すべきです。
> 
> 必ずしも GeoHash である必要はありません。

> [!TODO]
> 
> 最近は近傍探索と編集距離を検討しました。これは牛刀ですべてをぶったぎる手法です。よくありません。
> 
> 必要なのは以前との整合が取れるかのみです。
> 
> まずは以下のような手法を検討すべきです。
> 
> - 属性が似ており、バージョンが新しいオブジェクトを探す。
> - チェンジセットを確認し、「消されたオブジェクト」と「作成されたオブジェクト」を見る（[Find the id of a deleted node](https://wiki.openstreetmap.org/wiki/Find_the_id_of_a_deleted_node)）。
> - 同地点の過去時点データを比較する（[Attic Data](https://wiki.openstreetmap.org/w/index.php?title=Attic_Data)）。

### OSM ユニーク属性スキーム

OSM ユニーク属性を利用して再検索します。

同一施設と判断できる OSM オブジェクトが発見された場合、新しい OSM リンクを作成、リンク確立時刻を記録します。

## 人手確認フロー

### 対象

以下の場合、人手確認対象とします。

* 修復候補が存在しない
* 複数候補が同程度の確度を持つ
* 確度が閾値未満

システムはレビューキューへ登録します。

保持項目:

* Concept ID
* 旧 OSM ID
* 候補一覧
* 確度
* 発生時刻

### 通知

レビューキュー登録時は通知を生成します。

推奨配信方式:

* RSS 2.0
* Atom Feed
* JSON Feed

通知内容:

* Concept ID
* 機関名
* 問題種別
* 発生日時

## RDF モデリング指針

Concept を主語とします。

> [!TODO]
> 
> 千代田区役所を例に Turtle 記述を例示
