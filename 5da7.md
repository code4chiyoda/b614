---
slug: "5da7"
title: "千代田区市民機関データ仕様"
tracking_issue:
authors:
  - Nawashiro <rsm4905dc[a]mozmail.com>
status: draft
created: 2026-06-07
updated: 2026-06-07
---
# 千代田区市民機関データ仕様

> [!NOTE]
> 
> 本文書は Nawashiro が書いた草案です。改善できる箇所、より良い代替案を募集しています。

## 目的

本仕様は、千代田区内の市民機関・公共機関等の位置情報を含むデータセットを [Resource Description Framework](https://en.wikipedia.org/wiki/Resource_Description_Framework) として構築・維持するための識別子管理およびリンク修復手順を定義する。

本データセットは OpenStreetMap（以下 OSM）を主要データソースとするが、OSM ID は永続識別子ではないため、独自の概念識別子（Concept ID）を導入する。

## 基本原則

### 識別子の階層

各機関は以下の識別子を持つ。

| 種別          | 役割                     |
| ----------- | ---------------------- |
| Concept ID  | 施設概念の永続識別子             |
| External ID | 外部識別子（OSM 属性・GeoHash）  |
| OSM ID      | OSM のノード・ウェイ・リレーション ID |

Concept ID を主識別子とし、従属情報として外部識別子がある。OSM ID は外部識別子によって維持される。

## データ構造

### Concept データ

施設概念を表現する。

保持項目:

* Concept ID
* 外部識別子
* OSM リンク情報

### OSM リンク情報

Concept と OSM オブジェクトの対応履歴を管理する。

保持項目:

* OSM ID
* リンク確立時刻

複数履歴を保持することで過去の OSM ID を参照可能とする。

## Concept ID

### 要件

Concept ID は以下を満たさなければならない。

* 永続的であること
* OSM ID から独立していること
* 人間による運用が容易であること

### 推奨形式

> [!TODO]
> 
> 通し番号か、ASCII 文字列を検討すべきです。

## 外部識別子

### 目的

リンク修復に利用する。

### GeoHash

OSM オブジェクトの代表座標から算出する。

リンク修復の際、近傍の OSM データ抽出に用いる。

### OSM ユニーク属性（任意）

Wikidata ID 等、その OSM データを一意に識別できる属性がある場合に用いる。GeoHash よりも確実にリンクを修復できる。一部の OSM オブジェクトが対応している。

## 通常運用

利用者は Concept ID を参照する。

最新の有効な OSM リンクを取得し、外部サービスと連携を行う。

過去の OSM ID は削除せず履歴として保持する。

これは Wheelmap 等の外部システムとの同期遅延に対応するためである。

## リンク切れ検出

OSM オブジェクトが削除されたなどの理由で Overpass クエリ応答がない場合、リンク切れとみなす。リンク修復を行う。

## 修復スキーム

### GeoHash スキーム

1. GeoHash 近傍を抽出
2. 属性の編集距離を算出
3. 最適候補を選択

### OSM ユニーク属性スキーム

OSM ユニーク属性を利用して再検索する。

同一施設と判断できる OSM オブジェクトが発見された場合、新しい OSM リンクを作成、リンク確立時刻を記録する。

## 人手確認フロー

### 対象

以下の場合、人手確認対象とする。

* 修復候補が存在しない
* 複数候補が同程度の確度を持つ
* 確度が閾値未満

システムはレビューキューへ登録する。

保持項目:

* Concept ID
* 旧 OSM ID
* 候補一覧
* 確度
* 発生時刻

### 通知

レビューキュー登録時は通知を生成する。

推奨配信方式:

* RSS 2.0
* Atom Feed
* JSON Feed

通知内容:

* Concept ID
* 機関名
* 問題種別
* 発生日時

## RDF モデリング指針

Concept を主語とする。

> [!TODO]
> 
> 千代田区役所を例に Turtle 記述を例示
